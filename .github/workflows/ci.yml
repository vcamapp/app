name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

env:
  DEVELOPER_DIR: /Applications/Xcode_14.3.app

jobs:
  build:
    runs-on: macOS-13

    steps:
      - uses: actions/checkout@main
      - name: Test
        run: cd app/xcode && swift test
